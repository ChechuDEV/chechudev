<div>
	<h1>Hello, I'm Chechu</h1>
	<h2>A full-stack developer</h2>
</div>

<span class="shape" style="--n: 1"></span>
<span class="shape" style="--n: 2"></span>
<span class="shape" style="--n: 3"></span>
<span class="shape" style="--n: 4"></span>
<span class="shape" style="--n: 5"></span>

<style lang="scss">
	div {
		display: flex;
		flex-direction: column;
		justify-content: center;
		align-items: center;
		height: 100%;
		background-color: white;
		border-bottom: 0.125rem solid black;

		animation: reduceHeight 1s cubic-bezier(0.7, 0, 0.29, 1) 1s forwards;

		h1 {
			margin: 0.75em;
			line-height: 0;
			font-size: 3em;
			font-weight: 700;
		}
		h2 {
			margin: 0.75em;
			line-height: 0;
			font-size: 2em;
			font-weight: 600;
		}
	}

    .shape {
        display: block;
        position: absolute;
        bottom: calc(var(--n)*-50px);
        left: calc(var(--n)*50px);
        width: 50px;
        height: 50px;
        background-color: transparent;
        border-radius: .25rem;
        border: .25rem solid #bbb;

        animation: floatUp 1s ease-in-out calc(var(--n)*50ms) forwards;
    }

    @keyframes floatUp {
        0% {
        bottom: calc(var(--n)*-50px);
            transform: rotate(0deg);
        border: .25rem solid #bbb;

        }
        70% {
            bottom: calc(var(--n)*200px);
            transform: rotate(180deg);


        }
        100% {
        border: .25rem solid transparent;
            transform: rotate(360deg);

        }
    }

	@keyframes reduceHeight {
		0% {
			height: 100%;
		}

		100% {
			height: 300px;
		}
	}
</style>
